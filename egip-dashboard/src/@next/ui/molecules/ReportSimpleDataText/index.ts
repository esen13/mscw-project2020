import styled from 'styled-components';

const ReportSimpleDataText = styled.div`
  margin-top: 50px;
  font-size: 2em;
  font-family: 'Circe-Bold';
  color: ${({ theme }) => theme.colors.dashboardCard.defaultText};
`;

export default ReportSimpleDataText;
