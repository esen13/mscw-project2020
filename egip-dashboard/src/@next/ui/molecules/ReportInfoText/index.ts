import styled from 'styled-components';

const ReportInfoText = styled.div`
  margin-top: 280px;
  margin-bottom: 30px;
  color: ${({ theme }) => theme.colors.dashboardCard.defaultText};
`;

export default ReportInfoText;
