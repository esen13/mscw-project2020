import ANGLE from '@next/constants/angle';
import COUNT from '@next/constants/count';
import INDEX_OF_ARR from '@next/constants/index_of_arr';
import PERCENT from '@next/constants/percent';
import TIME from '@next/constants/time';

const CONSTANTS = {
  ANGLE,
  COUNT,
  INDEX_OF_ARR,
  PERCENT,
  TIME,
};

export default CONSTANTS;
